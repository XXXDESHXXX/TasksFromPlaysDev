version: '3.8'
services:
  custom-nginx:
    build:
      context: /home/ec2-user/TasksFromPlaysDev/Docker/Nginx_Conf/Task12
      dockerfile: Dockerfile
    ports:
      - "80:80"
      - "443:443"

  custom-apache:
    build:
      context: /home/ec2-user/TasksFromPlaysDev/Docker/Apache_Conf
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
